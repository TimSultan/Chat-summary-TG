.env
*.session
*.session-journal
__pycache__/
*.pyc
output/
cache/
history/
export/
voting/
.venv/
*.log
*.session_string*
session_string_output.txt
# The reference implementation the arena was ported from -- read alongside the code, not
# shipped with it. arena_core.py and the README cite it by path.
import/
sprite_preview/

# Local Claude Code tool permissions -- per-machine, not part of the project.
.claude/
